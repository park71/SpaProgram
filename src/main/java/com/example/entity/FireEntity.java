package com.example.entity;/*package hello.spaproject.entity;

import hello.spaproject.dto.FireDTO;
import jakarta.persistence.*;
import lombok.Getter;
import lombok.Setter;

import java.time.LocalDate;

@Getter
@Setter
@Entity
//@Table(name="index")
public class FireEntity {
    @Id
    private Long id;

    private String name;
    private String password;
    private String email;
    private String phone_num;
    private String school;
    private String year;
    private String major;

    public static FireEntity tofireEntity(FireDTO fireDTO) {
        FireEntity fireEntity = new FireEntity();
        fireEntity.setId(fireDTO.getId());
        fireEntity.setName(fireDTO.getName());
        fireEntity.setPassword(fireDTO.getPassword());
        fireEntity.setEmail(fireDTO.getEmail());
        fireEntity.setPhone_num(fireDTO.getPhone_num());
        fireEntity.setSchool(fireDTO.getSchool());
        fireEntity.setYear(fireDTO.getYear());
        fireEntity.setMajor(fireDTO.getMajor());

        return fireEntity;
    }
}*/
